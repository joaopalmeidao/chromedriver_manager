[project]
name = "chromedriver-version-manager"
version = "1.3.0"
description = "Chromedriver Version Manager"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "filelock>=3.20.3",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
